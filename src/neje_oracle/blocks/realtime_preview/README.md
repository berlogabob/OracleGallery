# Realtime Preview Block

Owns the live drawing preview projection. The current implementation is still
surfaced through the GUI support compatibility layer; future cleanup should move
runtime/queue projection and SVG preview rendering here.
