# Direct Print Block

Owns uploaded/file SVG direct-print use cases. The current implementation is
still surfaced through the GUI support compatibility layer; future cleanup should
move direct SVG validation, G-code creation, and upload payload parsing here.
